# Pinned for reproducible farm images. Bump deliberately, not by floating range.
# streamlit >= 1.37 is required for st.context.headers (the SSO identity gate).
streamlit==1.58.0
requests==2.32.5
urllib3==2.5.0
cryptography==46.0.7
